import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.taekwondo.sbeitla',
  appName: 'Taekwondo Sbeitla',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;

