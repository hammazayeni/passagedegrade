[build]
  base = "shadcn-ui"
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  VITE_BASE_PATH = "/"
  NODE_VERSION = "22"

[context.production.environment]
  VITE_BASE_PATH = "/"
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
